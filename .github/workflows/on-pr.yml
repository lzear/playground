name: On PR

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize

jobs:
  comment:
    uses: ./.github/workflows/comments-upsert.yml
    with:
      comment_body: Blablabla
      identifier: identifier-blabla
