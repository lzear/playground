name: On label

on:
  pull_request:
    types: [ labeled ]

jobs:
  comment:
    uses: ./.github/workflows/comments-upsert.yml
    with:
      comment_body: Last label set to ${{ github.event.label.name }}
      identifier: identifier-label
